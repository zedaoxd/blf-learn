/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        remotePatterns: [
            {
                hostname:  'sbruodoekbkbgiiyjxcc.supabase.co',
            }
        ]
    }
};

export default nextConfig;
